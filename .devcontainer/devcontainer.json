{
  "name": "sst-cam-proto",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspaces/sst-cam-proto",
  "customizations": {
    "vscode": {
      "extensions": [
        "anthropic.claude-code",
        "bufbuild.vscode-buf",
        "redhat.vscode-yaml",
        "drblury.protobuf-vsc"
      ]
    }
  },
  // Print the buf version so a fresh container immediately confirms it's on PATH.
  "postCreateCommand": "buf --version"
}
