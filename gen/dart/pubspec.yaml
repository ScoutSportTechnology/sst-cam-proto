# SST Cam Dart bindings — generated protobuf package.
#
# The app depends on this package by git tag (ref: vX.Y.Z, path: gen/dart).
# The `version` line below is managed by release-please (see
# release-please-config.json `extra-files`) and always equals the release tag.
name: sst_cam_proto
description: Generated Dart protobuf bindings for the SST Cam wire contract.
version: 0.0.0 # x-release-please-version
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  protobuf: ^3.1.0
