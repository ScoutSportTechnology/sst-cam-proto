// SST Cam proto — Dart barrel export.
//
// Re-exports the generated message/enum classes so consumers import a single
// package (`package:sst_cam_proto/sst_cam_proto.dart`).
//
// The `*.pb.dart` files referenced below are PRODUCED BY `buf generate`
// (via protoc-gen-dart) and committed to the repo — they are NOT hand-written.
// Run `just gen` inside the devcontainer to (re)generate them; CI's
// codegen-drift check fails if the committed output is stale.
export 'bluetooth.pb.dart';
export 'wifi.pb.dart';
